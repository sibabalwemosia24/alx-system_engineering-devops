echo Hello, world > 0-hello_world- prints "Hello, world" followed by a new line to the standard output
cat /etc/passwd- displays content of the /etc/passwd file
ls -lt | head /etc/passwd- displays the last ten lines of /etc/passwd
